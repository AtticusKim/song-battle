# Spotify API Credentials
# Get these from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Cron Job Secret (for protecting the refresh endpoint)
# Generate a random string for this (e.g., openssl rand -base64 32)
CRON_SECRET=your_random_secret_here

# Vercel KV (Redis) - Automatically populated by Vercel
# No need to set these manually, they're added when you create a KV store in Vercel
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=

# Database Configuration (for future implementation)
DATABASE_URL=postgresql://user:password@localhost:5432/song_battle

# Redis Configuration (for caching, future implementation)
REDIS_URL=redis://localhost:6379

# Application Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NODE_ENV=development
